# SharePoint Secure Configuration
# NOTE: same ciphertext as dev-secure-properties.yaml, since uat uses the same SharePoint site /
# Azure AD app / JKS keystore. This only decrypts correctly if the uat CloudHub deployment's
# mule.secure.key runtime property matches the key dev was encrypted with - verify on first deploy.
sharepoint:
  connection:
    keyStorePassword: "![fVLmPt+380wN3JXBiVfLCQ==]"
